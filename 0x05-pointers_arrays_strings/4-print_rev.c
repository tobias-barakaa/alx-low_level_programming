#include "main.h"

/**
 * main - function that check for 
 *
 * Return: Always 0.
 */
int main(void)
{
        char *str;

        str = "I do not fear";
        print_rev(str);
        return (0);
}
