#include <stdio.h>
#include "main.h"

/**
 * main - print number of prod
 * @argv: argrument first
 * @argc: second params
 */

int main(int argc, char **argv)
{
	if (argc > 0)
		printf("%d\n", argc);
			return (0);
}
