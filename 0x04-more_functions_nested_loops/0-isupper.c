#include "main.h"
#include <stdio.h>

/**
 * _isupper - functions to detect uppercase
 * @c: first argument
 * Return: always Return 0
 */

int _isupper(int c)
{'

	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else 
	{
		return (0);
	}
}
