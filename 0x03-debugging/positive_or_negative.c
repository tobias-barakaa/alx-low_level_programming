#include "main.h"

/**
 * positive_or_negative - function to run negative or positive
 *
 * @int - first argument
 * Return: Return 0 Always
 */

void positive_or_negative(int i)
{
	if(i < 0) 
	{
		printf("%d is negative", i)
	} else {
		printf("%d is positive", i)
	}
}
